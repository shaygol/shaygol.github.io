
"""
Alpha Scanner package.

Implements the core components described in the project plan:

- Factor computation modules (`factors`).
- Factor aggregation engine (`factor_engine`).
- Transaction cost & capacity model (`tccm`).
- Calibrator for factor weight optimization (`calibrator`).
- Optional LLM-based fundamentals & sentiment interface (`llm_module`).
"""

from . import factors  # noqa: F401


